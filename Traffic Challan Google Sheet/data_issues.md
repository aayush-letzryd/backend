# Traffic Challan Data Issues & Standardization Catalog

This document details all **15 operational abnormalities and data hygiene issues** identified across the multi-tab dataset of [`Traffic Challan details`](https://docs.google.com/spreadsheets/d/1jE6H8Uw0SLFgBKxnrFd9kHGNT26pFw0etiwpCeCrLQo/edit?usp=sharing), covering 38 weekly ledger cycles and historical monthly matrices (**35,930 total records** across **1,601 unique vehicles**).

---

## Catalog Summary Statistics
* **Total Tabs Consolidated**: 38 tabs (26 weekly cycles, 7 monthly matrices, 5 special penalty tabs)
* **Total Merged Records Scanned**: 35,930 records
* **Total Unique Vehicles Tracked**: 1,601 vehicles
* **Active Police Violations Incurred**: 5,043 fine events
* **Primary / Natural Key**: `(vehicle_reg_no, notice_no, week_cycle)`
* **Total Issues Documented**: 15 (`CHAL-01` through `CHAL-15`)
* **Zero Data Loss Rule**: 100% of historical weekly balances and violation events are preserved with full attribution.

---

## Master Issue Registry (`CHAL-01` to `CHAL-15`)

| Issue ID | Sheet / Tab | Variable / Column | Issue Name & Category | MY INPUT | Proposed Code Standardization Rule | Detailed Error Description & Root Cause | Affected Rows | % Dataset | Severity | Standardization Capability | Why Custom Input Needed (If Applicable) | Action Required by User / Ops Team |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: | :---: | :---: | :---: | :---: | :--- | :--- |
| **CHAL-01** | `Unified_Traffic_Challan_source` | `Registration No` | Primary Key Formatting & Whitespace Sanitization | Strip all spaces, hyphens, and dots, and convert to clean uppercase. Every plate must strictly follow standard 9-10 character format. | Apply `UPPER(TRIM(REGEXP_REPLACE(reg_no, r"[^A-Za-z0-9]", "")))`. Enforce 9-10 char plate standard. | Raw vehicle registration numbers contain internal spaces, lowercase letters, hyphens, and stray punctuation marks across weekly tabs. | 35930 | 100.0% | `CRITICAL` | `FULLY AUTOMATED (CODE)` | None (Regex uppercase & whitespace stripping handles all plate variations deterministically) | None |
| **CHAL-02** | `Unified_Traffic_Challan_source` | `City` | City Name Typos & Misspelling Normalization | Fix common typos like 'Hyderbad' to 'Hyderabad' and standardize case. If missing, auto-derive city from vehicle state prefix (KA -> Bangalore, TS/TG -> Hyderabad, MH -> Mumbai). | Map "Hyderbad" -> "Hyderabad", "blr"/"bang" -> "Bangalore", "mum" -> "Mumbai". Auto-derive missing cities from plate state prefixes. | City names entered with frequent spelling mistakes ('Hyderbad' in 4,943 rows), lowercase casing, or left blank across city operations. | 5069 | 14.1% | `HIGH` | `FULLY AUTOMATED (CODE)` | None (Typo dictionary and plate state prefix fallback automatically resolve city) | None |
| **CHAL-03** | `Unified_Traffic_Challan_source` | `City` | Shifted Numerical Fines in City Column | When numeric fine amounts appear in the City column due to column shifts, move the amount to challan fine and assign the city based on the plate prefix. | Detect numeric patterns in City column, route amount to `challan_amount`, and populate canonical city via vehicle plate prefix. | Spreadsheet column shifting caused fine amounts (500, 1000, 1500, 2000) to be entered into the City column instead of the Fine column. | 83 | 0.2% | `HIGH` | `FULLY AUTOMATED (CODE)` | None (Numeric detection in text column shifts value to fine and infers city from plate) | None |
| **CHAL-04** | `Unified_Traffic_Challan_source` | `Notice Date & Violation Date` | Non-Violation Placeholder String Handling | Convert placeholder tokens like '-', '--', 'NA', and 'None' in date fields into proper SQL NULLs without breaking the pipeline. | Convert literal placeholder tokens ('-', '--', 'NA', 'None') to SQL NULL without throwing date parsing exceptions. | Routine weekly balance snapshot rows for vehicles without new violations contain text hyphens and placeholder strings instead of valid dates. | 30917 | 86.0% | `MEDIUM` | `FULLY AUTOMATED (CODE)` | None (Token matcher safely converts placeholder strings to SQL NULL) | None |
| **CHAL-05** | `Unified_Traffic_Challan_source` | `Violation Date` | Multi-Format Violation Date Normalization | Parse mixed date formats (DD/MM/YYYY, YYYY-MM-DD, and Excel serial integers) into standard ISO YYYY-MM-DD date format. | Parse via regex & ISO standardizers into standard PostgreSQL `DATE` (YYYY-MM-DD). | Violation dates recorded across tabs using inconsistent date conventions, month-first formats, and raw spreadsheet numeric serial numbers. | 5013 | 14.0% | `HIGH` | `FULLY AUTOMATED (CODE)` | None (Multi-pattern parser normalizes DD/MM/YYYY, ISO, and serial dates) | None |
| **CHAL-06** | `Unified_Traffic_Challan_source` | `Notice Date` | Statutory Notice Issuance Date Parsing | Standardize official notice dates into YYYY-MM-DD format. Keep blank as NULL if the notice has not been officially generated yet. | Parse valid dates to `DATE` (YYYY-MM-DD); store `NULL` for unissued or pending notices. | Traffic police portal notice issuance dates formatted with mixed separators and missing values for offline or un-notified violations. | 4018 | 11.2% | `MEDIUM` | `FULLY AUTOMATED (CODE)` | None (Standard date parser casts valid dates and stores NULL for unissued notices) | None |
| **CHAL-07** | `Unified_Traffic_Challan_source` | `Time` | Violation Timestamp / Time Extraction | Extract violation time into standard 24-hour HH:MM:SS format. Store NULL for blank or hyphenated entries. | Extract clean `TIME` (HH:MI:SS); convert hyphens and invalid time tokens to SQL NULL. | Offense time recorded in varied formats including 12h AM/PM strings, Excel time objects, text hyphens, and missing cells. | 3343 | 9.3% | `MEDIUM` | `FULLY AUTOMATED (CODE)` | None (Time regex normalizes 12h/24h and converts non-time strings to NULL) | None |
| **CHAL-08** | `Unified_Traffic_Challan_source` | `Challen Amount` | Fine Amount Currency & String Cleaning | Remove currency symbols, commas, and trailing text, keeping only clean numeric fine amounts. Default non-violation rows to 0.00. | Apply `CAST(REGEXP_REPLACE(amount, r"[^0-9.]", "") AS NUMERIC(12,2))`. Default blanks/hyphens to 0.00. | Challan fine amounts contain rupee symbols ('₹500'), formatting commas ('1,000'), trailing decimals, and non-numeric string characters. | 5043 | 14.0% | `CRITICAL` | `FULLY AUTOMATED (CODE)` | None (Numeric sanitization strips currency symbols and casts to NUMERIC(12,2)) | None |
| **CHAL-09** | `Unified_Traffic_Challan_source` | `Sticker Fine` | Internal LetzRyd Compliance Penalty Isolation | Keep internal LetzRyd sticker penalties in a separate dedicated column so they don't mix with government traffic police challans. | Clean numeric penalty into dedicated `sticker_fine NUMERIC(12,2)` column without mixing with government police fines. | Internal vehicle branding / sticker non-compliance penalties recorded alongside government challan columns across weekly tabs. | 185 | 0.5% | `HIGH` | `FULLY AUTOMATED (CODE)` | None (Separate column mapping isolates internal sticker fines from government challans) | None |
| **CHAL-10** | `Unified_Traffic_Challan_source` | `Amount Paid` | Settlement & Payment Tracking Normalization | Extract cleared weekly settlement amounts as clean numbers to maintain accurate driver payment and deduction records. | Clean numeric paid amounts into `amount_paid NUMERIC(12,2)`. | Driver payment reconciliation entries contain mixed string representations, partial payment logs, and blank cells. | 2410 | 6.7% | `MEDIUM` | `FULLY AUTOMATED (CODE)` | None (Numeric parser extracts cleared payment amounts into amount_paid) | None |
| **CHAL-11** | `Unified_Traffic_Challan_source` | `Previous Balance (Upto Date)` | Rolling Balance Continuity & Header Date Parsing | Extract previous balance amounts as numeric values, and parse header date tags like 'Upto 19-07-26' into audit_date. | Extract rolling balance into `previous_balance NUMERIC(12,2)`; parse header date into `audit_date`. | Previous cumulative balance headers include embedded rolling date strings ('Upto 19-07-26', 'Updated On 23-8-26') instead of static column names. | 35930 | 100.0% | `MEDIUM` | `FULLY AUTOMATED (CODE)` | None (Regex header parser extracts cutoff dates into audit_date and amounts into previous_balance) | None |
| **CHAL-12** | `Unified_Traffic_Challan_source` | `Total pending` | Outstanding Balance Validation | Store the net closing balance and verify that it matches previous balance plus new fines minus amount paid. | Store as `total_pending NUMERIC(12,2)`; validate `total_pending = previous_balance + challan_amount - amount_paid`. | Total pending column reflects cumulative dues per vehicle, with occasional manual formula overwrites and manual entry discrepancies. | 24527 | 68.3% | `HIGH` | `HYBRID (CODE + OPS REVIEW)` | Arithmetic balance validation flags rows where closing balance does not equal previous balance + fine - paid. | Ops team to verify 47 historical discrepancy rows during weekly reconciliation audit. |
| **CHAL-13** | `Unified_Traffic_Challan_source` | `Notice Number (Primary Key)` | Deterministic Notice Number Resolution | For actual fines use the traffic notice ID or 'NOT-<Plate>-<Date>-<Amount>'; for routine weekly balance snapshots use 'BAL-<Plate>-<Week>' to guarantee unique primary keys. | For violations: construct unique notice identifier `'NOT-<REG>-<DATE>-<AMOUNT>'`; for weekly balance snapshots: construct `'BAL-<REG>-<WEEK>'`. | Routine weekly ledger snapshots lack unique police notice numbers, requiring deterministic synthetic keys to prevent primary key collision. | 35930 | 100.0% | `CRITICAL` | `FULLY AUTOMATED (CODE)` | None (Deterministic synthetic key generator creates unique keys for balance snapshot rows) | None |
| **CHAL-14** | `Unified_Traffic_Challan_source` | `Remarks` | Operational Notes & Payment Settlement Audit Strings | Clean quotes and extra spaces from remarks to preserve clear audit notes for payment disputes and ops tracking. | Sanitize quotes and trim whitespace; store clean `TEXT` for downstream audit reconciliation. | Free-text operational remarks contain special characters, line breaks, unescaped quotes, and inconsistent settlement tags. | 1420 | 4.0% | `LOW` | `FULLY AUTOMATED (CODE)` | None (Text sanitization cleans quotes and whitespace for audit trail) | None |
| **CHAL-15** | `Unified_Traffic_Challan_source` | `Multi-Tab Source` | Historical Monthly Grid Unpivoting | Unpivot historical 2025 monthly matrix tabs into individual violation event rows to match our standard weekly ledger schema. | Unpivot daily matrix grids into individual violation events with explicit `violation_date` and `challan_amount`. | Historical 2025 records were structured as horizontal date matrices across calendar days rather than vertical normalized event logs. | 367 | 1.0% | `HIGH` | `FULLY AUTOMATED (CODE)` | None (Cross-tab matrix unpivoting normalizes monthly grids into standard event ledger) | None |

---

## Team Lead Audit Resolution Log (Anurag Review Fixes 3.1 - 3.8)

| Issue # | Issue Title | Root Cause Identified | Resolved State & Implementation |
| :--- | :--- | :--- | :--- |
| **3.1** | Synthetic Notice Number Collisions | Synthetic notice format `NOT-{plate}-{date}-{amount}` collided on multiple fines per vehicle on the same day. | Updated synthetic generator to include violation time and sheet row number: `NOT-{plate}-{date}-{time}-{row}` for guaranteed uniqueness. |
| **3.2** | Hourly Trigger Syncs Wrong Tab (Jan 2025) | `ss.getActiveSheet()` defaulted to leftmost historical tab in headless time-driven background runs. | Created `resolveActiveWeekTab(ss)` to programmatically target active weekly tabs and `Unified_Traffic_Challan_source`. |
| **3.3** | 12-Hour AM/PM Time Parser Wipes Times | Regex failed on "10:30 PM" / "02:15 AM", wiping 3,343 violation times to NULL. | Added 12-hour AM/PM regex parser converting into clean 24-hour `HH:mm:ss`. |
| **3.4** | Shifted Fines in City Column Discarded | Leaked numeric values in City column were dropped due to empty conditional block. | Extracted numeric values from city column into `challan_amount` and derived canonical city from vehicle plate prefix. |
| **3.5** | Multi-Tab 6-Minute Quota Timeout | Iterating 38 tabs synchronously exceeded GAS 360-second limit without checkpointing. | Added state checkpointing via `PropertiesService` (`CHALLAN_SYNC_TAB_INDEX`) enabling resumable tab processing. |
| **3.6** | Multi-Row Paste Ignored in `handleOnEdit` | Line 625 only synced `e.range.getRow()`. | Added range loop from `e.range.getRow()` to `e.range.getLastRow()`. |
| **3.7** | Destructive Trigger Deletion in `deleteAllTriggers` | Wiped all project triggers indiscriminately. | Scoped deletion specifically to challan pipeline handler names (`handleOnEdit`, `syncCurrentWeekTab`, etc.). |
| **3.8** | Sequence Advancement on Conflict Upserts | Standard upsert burned sequence IDs on conflict checks. | Parameterized conflict updates preserving existing sequence numbers. |
